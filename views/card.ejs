<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sua Cartela de Bingo</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <link rel="icon" href="data:;base64,iVBORw0KGgo=">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md">
    <h1 class="text-2xl font-bold text-center mb-6">Sua Cartela de Bingo</h1>
    <p class="text-center mb-4">Número da Rifa: <strong><%= rifaNumber %></strong></p>
    <% if (card.buyerName && card.buyerPhone) { %>
      <p class="text-center mb-4">Comprador: <strong><%= card.buyerName %></strong> (<%= card.buyerPhone %>)</p>
    <% } %>
    <div class="grid grid-cols-5 gap-1 text-center font-bold">
      <div class="bg-blue-500 text-white p-2">B</div>
      <div class="bg-blue-500 text-white p-2">I</div>
      <div class="bg-blue-500 text-white p-2">N</div>
      <div class="bg-blue-500 text-white p-2">G</div>
      <div class="bg-blue-500 text-white p-2">O</div>
      <% for (let i = 0; i < 5; i++) { %>
        <div class="border p-2 <%= i === 2 && card.numbers.N.length === 4 ? 'bg-yellow-200' : '' %>">
          <%= card.numbers.B[i] || '' %>
        </div>
        <div class="border p-2 <%= i === 2 && card.numbers.N.length === 4 ? 'bg-yellow-200' : '' %>">
          <%= card.numbers.I[i] || '' %>
        </div>
        <div class="border p-2 <%= i === 2 && card.numbers.N.length === 4 ? 'bg-yellow-200' : 'Livre' %>">
          <%= i === 2 && card.numbers.N.length === 4 ? 'Livre' : card.numbers.N[i] || '' %>
        </div>
        <div class="border p-2 <%= i === 2 && card.numbers.N.length === 4 ? 'bg-yellow-200' : '' %>">
          <%= card.numbers.G[i] || '' %>
        </div>
        <div class="border p-2 <%= i === 2 && card.numbers.N.length === 4 ? 'bg-yellow-200' : '' %>">
          <%= card.numbers.O[i] || '' %>
        </div>
      <% } %>
    </div>
    <a href="/display" class="block mt-4 bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600 text-center">Ver Sorteio</a>
  </div>
</body>
</html>
