<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bingo Display</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Prêmio Atual</h1>
  <p><%= game.currentPrize || '--' %></p>

  <h1>Informações Adicionais</h1>
  <p><%= game.additionalInfo || '--' %></p>

  <h1>Último Número Sorteado</h1>
  <p><%= game.lastNumber || '--' %></p>

  <h1>Últimos 5 Números Sorteados</h1>
  <p><%= game.drawnNumbers.slice(-5).join(', ') || '--' %></p>
</body>
</html>
